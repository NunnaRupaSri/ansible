- name: Copy Mongo Repo
  copy:
      src: mongo.repo
      dest: /etc/yum.repos.d/mongo.repo

- name: Install MongoDB
  package: 
    name: mongodb-org
    state:  present
  
- name: Start MongoDB
  service:
    name: mongod
    state: started
    enable: yes


