- name: Demo of Conditionals
  hosts: all
  tasks:
 - shell: /bin/true
    register: result

 - debug:
    msg: Nice, Good Job
    when: result is succeeded.

 - debug:
    msg: Bad, Try Again
    when: result is Failed.